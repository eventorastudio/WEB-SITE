# Backups locales con datos personales generados antes de una migración.
backups/

# Dependencias y credenciales administrativas locales.
node_modules/
functions/generated/
*.service-account.json
service-account*.json

# Artefactos locales de Firebase CLI / Emulator Suite.
.firebase/
firebase-debug.log*
firestore-debug.log*
storage-debug.log*
ui-debug.log*

# Evidencias temporales generadas por Microsoft Playwright MCP.
.playwright-mcp/
